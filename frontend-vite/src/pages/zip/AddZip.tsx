const HandleUpload = async () => {

};


const AddZip = () => {
    interface File {
        name: string,
    }

    return (
        <div>
            <h1>Upload a new zip file</h1>

            <form onSubmit={HandleUpload}>
                <input


                />
            </form>
        </div>
    );
};

export default AddZip;